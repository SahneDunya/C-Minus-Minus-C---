#include "c--_data_types.h"
#include <stdio.h> 

// Örnek bir yardımcı fonksiyon (şu an sadece bir taslak):
 const char* cmm_bool_to_string(cmm_bool value) {
    return value == CMM_TRUE ? "true" : "false";
}